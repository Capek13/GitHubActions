name: 04 secound workflow

on:
  workflow_run:
    workdlows: ["04 workflow_dispatch"]
    types:
      - completed

jobs:
  echo-hi-all:
    runs-on: ubuntu-latest
    steps:
      - name: Echo "Hi all"
        run: |
          echo "Hi all"